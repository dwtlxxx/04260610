# 面试环境就绪清单（现场照着确认）

> 生成时间：面试前准备阶段　|　仓库：https://github.com/dwtlxxx/04260610

---

## 一、已完成（我这边做完的）

| # | 项目 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 工作区目录整理 | ✅ | 所有资料统一收进子文件夹，不污染仓库根目录 |
| 2 | Git 仓库初始化 | ✅ | `main` 分支，remote 指向 `git@github.com:dwtlxxx/04260610.git` |
| 3 | 仓库级 Git 身份 | ✅ | `dwtlxxx` / `dwtlxxx@users.noreply.github.com` |
| 4 | SSH 密钥生成 | ✅ | 存于 `_keys/`（已被 `.gitignore` 拦截，不会进仓库） |
| 5 | SSH 通道连通性 | ✅ | 端口 22 与 443 均通 |
| 5.1 | **SSH 公钥已授权** | ✅ | 实测返回 `Hi dwtlxxx! You've successfully authenticated` |
| 5.2 | **Pages 发布方案已定** | ✅ | 方案 1：根目录 `index.html` + `main` 分支 `/(root)` |
| 6 | `.gitignore` 安全红线 | ✅ | 拦截密钥、Token、`.env`、依赖、构建产物 |
| 7 | README 骨架 | ✅ | 含完整的填写位（产品名/问题/功能/说明/自研内容） |
| 8 | 部署手册 | ✅ | `docs/02-GitHub部署手册.md`，含故障速查表 |
| 9 | 协作规范 | ✅ | `docs/01-开发流程与约定.md` |
| 10 | `.nojekyll` | ✅ | 避免下划线目录被 Jekyll 忽略导致 404 |
| 11 | 网络劫持排查 | ✅ | 原 hosts 被 Steam++ 劫持至 127.0.0.1，已恢复真实 IP |
| 12 | Pages 域名解析 | ✅ | `dwtlxxx.github.io` → 185.199.110.153（GitHub 官方 IP，正常） |

---

## 二、已确认的关键决策（已锁定）

### A. SSH 公钥 —— ✅ 已添加并实测通过

```
$ ssh -T git@github.com
Hi dwtlxxx! You've successfully authenticated, but GitHub does not provide shell access.
```

> 注：该命令返回退出码 1 属正常现象，只要看到 `successfully authenticated` 即成功。

### B. Git 提交身份 —— ✅ 已锁定

`dwtlxxx` / `dwtlxxx@users.noreply.github.com`（GitHub 隐私邮箱，不暴露真实邮箱）

### C. Pages 发布策略 —— ✅ 已选方案 1

**方案 1：根目录直接发布**（零构建、零依赖、零翻车风险）

| 项目 | 约定 |
| --- | --- |
| 站点入口 | **仓库根目录的 `index.html`**（必须叫这个名字，且必须在这一层） |
| Pages 配置 | Settings → Pages → Source: `Deploy from a branch` → Branch: `main` → Folder: `/(root)` |
| 访问地址 | https://dwtlxxx.github.io/04260610/ |
| 子目录访问 | 若内容放在 `todo-app/`，地址为 `.../04260610/todo-app/`（**可访问，但入口链接必须由根 `index.html` 提供**） |

**由方案 1 推导出的硬性目录约束（开发时必须遵守）：**

1. `index.html` 必须位于**仓库根目录**，这是站点首页
2. 所有资源引用必须使用**相对路径**（`./css/style.css`、`./js/app.js`），
   严禁写成 `/css/style.css` —— 绝对路径会指向 `dwtlxxx.github.io/css/...` 导致 404、样式全丢
3. 页面间跳转用相对路径（`./detail.html`）
4. 项目源码若放入子文件夹，根目录 `index.html` 需提供入口跳转或直接作为作品本体
5. **不引入任何构建步骤**：不出现 `npm run build`，写什么就提交什么

---

## 三、仓库当前状态

**正式作品已完成、已推送、线上实测通过。** 提交历史（本地 `main`，共 18 条提交，其中前 15 条为第一阶段）：

```
a752123  docs: README 补充智能搜索与搜索理解条，并新增交付文档自检脚本
68b4543  test: 增加搜索召回/精度自检与搜索局部刷新回归测试
e495d70  feat: 重写智能搜索（拼音/首字母/近义词/错字容错）并加搜索理解条
038b294  feat: 恢复题目提供的 26 条校园信息，正式投入交付
bb63b73  docs: 修正 README 格式与过期内容，移除失效的验证脚本
cda3f38  fix: 修复发布按钮无反应，并实现模糊搜索
b90e83c  feat: AI 助手移至右栏底部、排序栏改「多列卡片」、彻底移除表格模式
bbcf2f7  feat: 侧栏开合动画、信息流改双列卡片、修复关联项空徽章与 AI 面板显示
7a898fa  fix: 修复 getRawItems 重复声明导致模块加载失败
7781f1d  feat: 新增帖子关联系统，在帖子顶部展示相关内容并可跳转
a0c87d1  fix: 修复详情弹层报错与弹层内容滞后两个缺陷
3e3830d  fix: 手机端顶栏品牌居中，并补齐 9 档分辨率适配
7491974  feat: 完善交互反馈与动画，修复搜索输入中断问题
9f52c48  feat: 新增 AI 整合模块，修复弹层失效，重构响应式与时间线
77c75a8  fix: 修复 8 处渲染与行为缺陷
62a4629  feat: 增加标签制分板块、置顶机制与时间线信息流
8234ec6  fix: 修复模块导出缺失导致页面卡在加载界面
18db180  feat: 实现校园机会雷达双端界面与信息质量引擎
d10ba0e  chore: 初始化项目仓库   ← 远端已有的起点
```

### 数据状态（重要）

| 项目 | 状态 |
| --- | --- |
| 题目数据 | ✅ **26 条已全部恢复并接入**（`data.js` 的 `ITEMS`） |
| 列表可见条数 | 24 条（09、20 两条纯补充通知并入主条目后隐藏） |
| 演示数据 | 已停用并清空（`USE_DEMO_DATA = false`，`DEMO_ITEMS = []`） |
| 1 + 9 关联 | ✅ 合并生效：活动时间 **9/21 19:30** / 地点 **实验楼 A402** |
| 3 + 20 关联 | ✅ 岗位状态：**已满 开发** / **仍在招 设计、材料** |
| 每条补充字段 | `tags`（主题标签）、`boards`（板块归属）、`pin`（置顶+理由） |

### 验证结果（每次提交前实测）

```
架构自检（7 档分辨率）  360/480/768/1024/1280/1440/1600px   全部通过
渲染审查                CSS 变量 / 类名 / 标签配平 / 重复 id / XSS   0 错误 0 警告
交互验证                发布 / 收藏 / 留言 / 举报 / 关联 / AI / 搜索   77 项全部通过
搜索自检                中文·拼音·首字母·错字·近义词·多词 召回 + 无关词零命中   44 项全部通过
交付文档自检            README 表格列数 / 必需链接 / 过期内容   19 项全部通过
线上实测                12 个文件与本地逐字节一致 + 9 个模块依赖图闭合   23 项全部通过
```

**Pages 地址**：https://dwtlxxx.github.io/04260610/
**线上状态**：✅ 已构建完成（实测文件内容与本地提交完全一致）

### 上传命令

```powershell
# 本地提交（若还有未提交改动）
git add -A ; git commit -m "说明"
# 推送
git push origin main
# 等 1~2 分钟让 Pages 重新构建，然后实测线上
node tools/check-live.mjs
```

> 推送为**快进关系**，普通 push 即可，无需 force。已实测推送成功。

---

### 仓库设置（Pages 发布）—— 已确认可用

1. <https://github.com/dwtlxxx/04260610/settings/pages>
2. **Source** 选 `Deploy from a branch`
3. **Branch** 选 `main`，**Folder** 选 `/(root)` → **Save**
4. 站点已能正常访问：https://dwtlxxx.github.io/04260610/

---

## 四、沙箱环境的能力边界（我需要你知道）

我的执行环境对 **Windows 命名管道**有限制，导致：

| 操作 | 沙箱内 | 你的终端 |
| --- | --- | --- |
| 读写工作区文件 | ✅ 可以 | ✅ 可以 |
| `git init` / `add` / `commit` / `log` / `status` | ✅ 可以 | ✅ 可以 |
| `git push` / `pull` / `ls-remote`（任何网络操作） | ⚠️ 受限沙箱下被阻断；**放宽权限后实测可推送**（已成功推送到 `a752123`） | ✅ 可以 |
| 编辑 GitHub 仓库设置、开 Pages | ❌ 无法代做 | ✅ 可以 |
| 线上 Pages 自查 | ✅ 可以（`node tools/check-live.mjs`，逐字节比对 + 依赖图校验） | ✅ 也可以 |

**结论**：编码、文档、本地提交、推送、线上实测我都能独立完成。若某次 push 报
`sh.exe: couldn't create signal pipe, Win32 error 5`，那是沙箱在拦命名管道，
把该条命令单独放宽权限重跑一次即可，**不是密钥或网络坏了**。

---

## 五、给你的一句话总结

基础设施已**全部就绪、推送成功、线上实测通过**。直接把题目丢给我即可——我会按「需求拆解 → 你确认 → 编码 → 本地 Commit → 推送 → 线上实测」的节奏推进，每个里程碑都交付一个**可打开、可操作**的版本。

### 现场速查：四条命令走完交付

```powershell
# 1. 环境自检（网络 / SSH / 密钥 / Git / 远端）
powershell -ExecutionPolicy Bypass -File "D:\desktop\Computer progream\tools\check-env.ps1"

# 2. 提交前自检（架构 / 渲染 / 交互 / 搜索 / README，全绿再提交）
node tools/verify-arch.mjs ; node tools/verify-render.mjs ; node tools/verify-interaction.mjs ; node tools/check-search.mjs ; node tools/check-readme.mjs

# 3. 提交并推送（-Message 可省略，省略时只推送已提交内容）
powershell -ExecutionPolicy Bypass -File "D:\desktop\Computer progream\tools\push.ps1" -Message "feat: 完成核心功能"

# 4. 线上实测（等 1~2 分钟让 Pages 重建）
node tools/check-live.mjs
```

### 与 Steam++（Watt Toolkit）加速共存 —— 已解决

**推送不受影响**：本仓库用 SSH 推送，SSH 协议不经 TLS，Steam++ 的中间人代理拦不到。
即使 hosts 又把 `github.com` 指向 `127.0.0.1`，push 依然正常。

**HTTPS 类工具也修好了**，且**证书校验保持开启**（不是简单关校验）：

```
core.sshCommand   = ssh -F _keys/gh_ssh_config     # SSH 推送，不受加速影响
http.sslBackend   = openssl
http.sslCAInfo    = _keys/steamtools-ca.pem        # 显式信任 Steam++ 的自签证书
```

Steam++ 想开就开着，浏览 GitHub 照样加速；需要核验远端时用：

```powershell
powershell -ExecutionPolicy Bypass -File "tools\check-remote.ps1"
```

### 已解决的两个关键故障（现场若再遇到可照此排查）

| 故障现象 | 根因 | 解决 |
| --- | --- | --- |
| `Identity file D:/desktop/Computer not accessible`<br>`Host key verification failed` | Git 解析 `core.sshCommand` 时**不做引号处理**，只按空格切分；而工作区路径 `Computer progream` 含空格，导致 `-i` 收到被截断的路径 | 改为 `core.sshCommand = ssh -F _keys/gh_ssh_config`（零空格），密钥与主机密钥路径写进 ssh 配置文件内部 |
| 脚本报 `The string is missing the terminator` 等语法错误 | `.ps1` 文件无 UTF-8 BOM，Windows PowerShell 5.1 按 GBK 解析中文 | 两个脚本已保存为 UTF-8 with BOM |

